rootProject.name = 'miniconnect'

include 'api'
include 'jdbc'
include 'jdbc-adapter'
include 'lang'
include 'messenger'
include 'rdbms-framework'
include 'tool'
include 'server'
include 'transfer'
include 'util'

rootProject.children.each { project ->
    project.projectDir = new File(settingsDir, "projects/${project.name}")
}
